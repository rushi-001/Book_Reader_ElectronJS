import { GalleryBooks } from "../GalleryBooks";

export const RecentlyAddedBooks = () => {
    return (
        <div>
            <h2>Recently Added Books</h2>
            <GalleryBooks />
        </div>
    );
}